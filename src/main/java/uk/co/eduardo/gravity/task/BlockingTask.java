package uk.co.eduardo.gravity.task;

/**
 * Describes an object that implements a blocking task, i.e. a task that disallows user input until it is complete.
 *
 * @author Ed
 */
public interface BlockingTask
{
   // Marker interface.
}
